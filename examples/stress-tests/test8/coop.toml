name = "test8"
version = "0.1.0"

# Test 8: Postgres SELECT — calls into Perry's pg stdlib.
# Reads from a test table populated by the migration runner.

[hosts]
domains = ["coop-test8.perryts.com"]

[[handlers]]
file = "handlers/main.ts"
path = "/"
method = "GET"
