name = "test1"
version = "0.1.0"

# Test 1: minimal handler, no allocations beyond response object.
# Baseline for "what does the smallest deployment cost?"

[hosts]
domains = ["coop-test1.perryts.com"]

[[handlers]]
file = "handlers/main.ts"
path = "/"
method = "GET"
