name = "test5"
version = "0.1.0"

# Test 5: large response — generates a 50 KB JSON payload.
# Measures large response building + base64 encoding overhead.

[hosts]
domains = ["coop-test5.perryts.com"]

[[handlers]]
file = "handlers/main.ts"
path = "/"
method = "GET"
